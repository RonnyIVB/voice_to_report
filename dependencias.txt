Meta
app name: n8n_ia_sql

Transcripcion
	pip install -U openai-whisper
	choco install ffmpeg (admin)
Text to SQL
	Chromadb:
		pip install chromadb
		pip install -q -U google-generativeai

	Pinecode:
		pip install grpcio-tools==1.71.0
		pip install pinecone
		pip install google-genai
		pip install python-dotenv

	Ollama:
		# Instalar ollama en Windows
		irm https://ollama.com/install.ps1 | iex
			# Comando en linux
			curl -fsSL https://ollama.com/install.sh | sh

			# Descargar el modelo Gemma 4 4B (E4B - Edge), este son 9.6GB
			ollama pull gemma4:e4b
			# Ejecutar modelo. Use Ctrl + d or /bye to exit.
			ollama run gemma4:e4b

		# Instalar librería
		pip install ollama
			

Entendido, acabo de instalar la librería ollama. Planifiquemos los pasos siguientes para utilizar el modelo Gemma 4 4B (E4B - Edge) en esta aplicación.

